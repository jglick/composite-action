name: 'test'
description: 'Testing'
runs:
  using: "composite"
  steps: 
    - run: $GITHUB_ACTION_PATH/action/script1.sh
      shell: bash
    - name: Check out
      uses: actions/checkout@v1
    - name: Set up JDK 8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - run: $GITHUB_ACTION_PATH/action/script2.sh
      shell: bash
